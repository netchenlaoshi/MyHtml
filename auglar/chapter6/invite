<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>invite</title>
</head>
<body>
    <ng-click="invite()">
        <button ng-click="invite()" class="btn btn-success">邀请</button>
<script src="lib\angular.min.js"></script>
<script>
 $scope.invite = function () {
        modelService.add(guestInfo.name, guestInfo.phone);
}
model.factory('modelService',function(){
    var guestList={
        list:[],
        add:function(name,phone);
        this list.push(guest);
        $scope.guest=modelService.guestList(state);
    }
});
<tr ng-repeat="guest in guests">
        <td>{{$index+1}}</td>
        <td>{{guest name}}</td>
        <td>{{guest phone}}</td>
        <td>{{guest state}}</td>
</tr>
    
</script>   
</body>
</html>
